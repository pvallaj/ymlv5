import { TipoPago.Pipe.TsPipe } from './tipo-pago.pipe.ts.pipe';

describe('TipoPago.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPago.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
